{
  "name": "Superbig Beam",
  "install": "./scripts/start-docker.sh && docker compose build && composer install --no-interaction --no-progress",
  "start": "./scripts/start-docker.sh && docker compose up -d"
}

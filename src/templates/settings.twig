{% import "_includes/forms" as forms %}

{{ forms.selectField({
    label: "Temp Filesystem"|t('beam'),
    instructions: "Select a filesystem volume to use for temporary file storage. This is useful for load-balanced environments where the local filesystem is not shared between servers. Leave empty to use the local temp directory."|t('beam'),
    id: 'tempFilesystemHandle',
    name: 'tempFilesystemHandle',
    options: {'': 'Local Temp Directory (Default)'|t('beam')} | merge(filesystems),
    value: settings.tempFilesystemHandle,
    warning: settings.tempFilesystemHandle ? "Make sure the selected filesystem is accessible by all servers in your load-balanced environment."|t('beam') : null,
}) }}

{{ forms.textField({
    label: "Temp Subfolder"|t('beam'),
    instructions: "The subfolder path within the filesystem to use for temp files. Defaults to 'beam'."|t('beam'),
    id: 'tempSubfolder',
    name: 'tempSubfolder',
    value: settings.tempSubfolder ?? 'beam',
}) }}

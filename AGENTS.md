# Cursor Cloud specific instructions

- Run the Pest suite with `make test` (MySQL) or `make test-postgres`. Local Docker PHP is 8.5; GitHub Actions also covers 8.2–8.4.
- Run all tests with `make test-all`.
- Run coding standards with `make ecs` and static analysis with `make phpstan`.
